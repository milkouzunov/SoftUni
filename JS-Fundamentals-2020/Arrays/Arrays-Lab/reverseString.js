function solve(input) {
   console.log(input.reverse().join(" "));
}

function swap(arr) {
  for(let i = 0; i < arr.length / 2; i++) {
    let temp = arr[i]  
  }
}

solve(['a', 'b', 'c', 'd', 'e']);
swap(['a', 'b', 'c', 'd', 'e']);